// fullName();

// longestWord();

// alphabeticString();

// extractMonth();

// let len = prompt("enter the len of rounded num< should be less than 10")
// randomArray(len);

// sorting();

// getMaxMin();

// removeRepeated();   

// copyArray();